# Render PostgreSQL connection string
DATABASE_URL=postgres://user:password@host:5432/dbname

# Optional: per-table read limit for schema tables (accounts, clients, tax_sales, parcels)
DATA_ROW_LIMIT=50

# OpenAI secrets
OPENAI_API_KEY=
OPENAI_ASSISTANT_ID=
